apiVersion: v1
kind: Pod
metadata:
  name: bad-pod-root
  labels:
    app: insecure-app
spec:
  containers:
    - name: nginx
      image: nginx:latest
      securityContext:
        runAsUser: 0
      # Missing securityContext defaults to root -> FAIL