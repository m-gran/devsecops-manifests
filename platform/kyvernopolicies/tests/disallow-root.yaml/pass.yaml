apiVersion: v1
kind: Pod
metadata:
  name: good-pod-root
  labels:
    app: secure-app
spec:
  containers:
    - name: nginx
      image: nginx:latest
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000